import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main:"#6d4c41"
        },
        secondary: {
            main:'#fafafa'
        }
    }
})

export default theme